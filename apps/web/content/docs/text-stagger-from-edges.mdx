export const meta = {
  title: "Edge Stagger",
  description:
    "Characters start from both edges and converge toward the center.",
  toc: [
    { label: "Preview", id: "preview" },
    { label: "Usage", id: "usage" },
    { label: "Props", id: "props" },
    { label: "Composition", id: "composition" },
  ],
}

## Preview

<Preview id="TextStaggerFromEdges" />

<EditorLink id="TextStaggerFromEdges" />

## Usage

Characters start from both edges and converge toward the center. Drop it in as a chapter card, hero opener, or as a beat between scenes — the same headline + subtitle props power the whole text-animation family, so swapping motions takes one field change.

## Props

<PropsTable id="TextStaggerFromEdges" />

## Composition

<CompositionStats id="TextStaggerFromEdges" />
