export const meta = {
  title: "Focus Pull",
  description:
    "A premium focus pull from heavy blur to crisp text, then a soft blur-out exit.",
  toc: [
    { label: "Preview", id: "preview" },
    { label: "Usage", id: "usage" },
    { label: "Props", id: "props" },
    { label: "Composition", id: "composition" },
  ],
}

## Preview

<Preview id="TextFocusBlurResolve" />

<EditorLink id="TextFocusBlurResolve" />

## Usage

A premium focus pull from heavy blur to crisp text, then a soft blur-out exit. Drop it in as a chapter card, hero opener, or as a beat between scenes — the same headline + subtitle props power the whole text-animation family, so swapping motions takes one field change.

## Props

<PropsTable id="TextFocusBlurResolve" />

## Composition

<CompositionStats id="TextFocusBlurResolve" />
